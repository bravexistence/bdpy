name: CI Workflow
on:
  push:
    branches:
      - main
      - CI_branch

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: python3 test_task_Morozov_Vitaly_prime_checksum.py
      - run: echo OK